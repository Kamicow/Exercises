#include <iostream>
using namespace std;
	int main() {
		float x, y;
		
		cout << "Bir sayı giriniz ";
		cin >> x;
		
		cout << "Bir sayı giriniz ";
		cin >> y;
		
		cout << (float)x + (float)y << "\n";
		cout << (float)x - (float)y << "\n";
		cout << (float)x * (float)y << "\n";
		cout << (float)x / (float)y << "\n";
	}
